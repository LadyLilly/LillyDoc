/**
 * LillyOptions interface, contains all options regarding Library
 */
export interface LillyOptions {
    containerId: string;
    inputId: string;
    listId: string;
    debounceTime?: number;
    maxResults?: number;
}
